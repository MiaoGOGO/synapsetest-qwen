<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.synapsetest.testmanagement.mapper.QualityReportMapper">

    <!-- Result Map for QualityReport -->
    <resultMap id="QualityReportResultMap" type="com.synapsetest.testmanagement.model.QualityReport">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="name" column="name"/>
        <result property="summary" column="summary"/>
        <result property="testResults" column="test_results" typeHandler="com.synapsetest.testmanagement.config.JsonTypeHandler"/>
        <result property="defectStats" column="defect_stats" typeHandler="com.synapsetest.testmanagement.config.JsonTypeHandler"/>
        <result property="performanceMetrics" column="performance_metrics" typeHandler="com.synapsetest.testmanagement.config.JsonTypeHandler"/>
        <result property="riskAssessment" column="risk_assessment" typeHandler="com.synapsetest.testmanagement.config.JsonTypeHandler"/>
        <result property="generatedAt" column="generated_at"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- Select by ID -->
    <select id="selectById" resultMap="QualityReportResultMap">
        SELECT * FROM quality_reports WHERE id = #{id}
    </select>

    <!-- Select All -->
    <select id="selectAll" resultMap="QualityReportResultMap">
        SELECT * FROM quality_reports ORDER BY generated_at DESC
    </select>

    <!-- Select by Task ID -->
    <select id="selectByTaskId" resultMap="QualityReportResultMap">
        SELECT * FROM quality_reports WHERE task_id = #{taskId} ORDER BY generated_at DESC
    </select>

    <!-- Select by Status -->
    <select id="selectByStatus" resultMap="QualityReportResultMap">
        SELECT * FROM quality_reports WHERE status = #{status} ORDER BY generated_at DESC
    </select>

    <!-- Select by Generated At Between -->
    <select id="selectByGeneratedAtBetween" resultMap="QualityReportResultMap">
        SELECT * FROM quality_reports 
        WHERE generated_at BETWEEN #{startTime} AND #{endTime}
        ORDER BY generated_at DESC
    </select>

    <!-- Select Top 10 by Order by Generated At Desc -->
    <select id="selectTop10ByOrderByGeneratedAtDesc" resultMap="QualityReportResultMap">
        SELECT * FROM quality_reports 
        ORDER BY generated_at DESC 
        LIMIT 10
    </select>

    <!-- Insert -->
    <insert id="insert" parameterType="com.synapsetest.testmanagement.model.QualityReport">
        INSERT INTO quality_reports(
            id, task_id, name, summary, test_results, defect_stats,
            performance_metrics, risk_assessment, generated_at, status,
            created_at, updated_at
        ) VALUES (
            #{id},
            #{taskId},
            #{name},
            #{summary},
            #{testResults, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            #{defectStats, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            #{performanceMetrics, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            #{riskAssessment, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            #{generatedAt},
            #{status},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <!-- Update -->
    <update id="update" parameterType="com.synapsetest.testmanagement.model.QualityReport">
        UPDATE quality_reports SET
            task_id = #{taskId},
            name = #{name},
            summary = #{summary},
            test_results = #{testResults, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            defect_stats = #{defectStats, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            performance_metrics = #{performanceMetrics, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            risk_assessment = #{riskAssessment, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            generated_at = #{generatedAt},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- Delete by ID -->
    <delete id="deleteById">
        DELETE FROM quality_reports WHERE id = #{id}
    </delete>

    <!-- Delete by Task ID -->
    <delete id="deleteByTaskId">
        DELETE FROM quality_reports WHERE task_id = #{taskId}
    </delete>

    <!-- Count -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM quality_reports
    </select>

</mapper>

